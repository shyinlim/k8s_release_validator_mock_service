apiVersion: v1
kind: Service
metadata:
  name: mock-service
spec:
  selector:
    app: mock-service # Which Pods this Deployment manages (match by label)
  ports:
    - port: 80 # Port exposed by the Service
      targetPort: 7641 # Port the container listens on